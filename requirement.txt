# Core LangChain
langchain>=0.3
langchain-community>=0.3
langchain-core>=0.3
langchain-openai>=0.2
langchain-text-splitters>=0.3
langchain-chroma>=0.1
langchain-neo4j>=0.1

# LLM SDKs
openai>=1.0
anthropic

# Local embeddings — BAAI/bge-small-en-v1.5 (offline; replaces Gemini + NVIDIA NIM)
sentence-transformers>=2.7.0
torch

# Vector DB
chromadb>=0.5

# Graph DB
neo4j>=5.0

# Document loaders
pypdf>=4.0
pymupdf>=1.24
pytesseract>=0.3
pdf2image>=1.17
Pillow>=10.0
beautifulsoup4>=4.12
lxml>=5.0
unstructured>=0.14

# NLP — entity & relationship extraction
spacy>=3.7
# After install: python -m spacy download en_core_web_sm

# Web framework (FastAPI gateway)
fastapi>=0.100
uvicorn[standard]>=0.23

# Hybrid search — BM25 keyword ranking (Reciprocal Rank Fusion with vector search)
rank-bm25>=0.2.2

# Utilities
python-dotenv
pydantic>=2.0
pydantic-settings>=2.0
tqdm
typer>=0.12

# Testing
pytest>=8.0
pytest-mock>=3.12
httpx>=0.27          # required by fastapi.testclient

# Optional — pluggable session backend
# redis>=5.0         # uncomment to enable CHAT_API_SESSION_BACKEND=redis

# ── MOSDAC agent stack (mosdac_agent/*) ─────────────────────────────────────
# Required for the LangGraph ReAct agent (in-process tools mode)
langgraph>=0.2
# Required for the MCP server entrypoint (python -m mosdac_agent.mcp_server)
fastmcp>=2.5
# Required only when AGENT_USE_LOCAL_TOOLS=false (agent talks MCP over HTTP)
# langchain-mcp-adapters>=0.1
# Optional UI prototype
streamlit>=1.36
requests>=2.31
